# Generate a secret with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
# Each demo password must be at least 12 characters.
ADMIN_L1_PASSWORD=
ADMIN_L2_PASSWORD=
ADMIN_L3_PASSWORD=

# Set true only when serving through HTTPS (local HTTP needs false).
COOKIE_SECURE=false
